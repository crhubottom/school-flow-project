node_modules/
.DS_Store
.env
<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>School Flow — Login</title>
    <meta name="description" content="School Flow login with Google (Firebase Authentication)" />
    <style>
      :root {
        --bg: #0f172a; /* slate-900 */
        --panel: #111827; /* gray-900 */
        --text: #e5e7eb; /* gray-200 */
        --muted: #9ca3af; /* gray-400 */
        --brand: #2563eb; /* blue-600 */
        --brand-hover: #1d4ed8; /* blue-700 */
        --success: #10b981; /* emerald-500 */
        --danger: #ef4444; /* red-500 */
      }
      * { box-sizing: border-box; }
      html, body { height: 100%; }
      body {
        margin: 0;
        font-family: system-ui, -apple-system, Segoe UI, Roboto, Helvetica, Arial, "Apple Color Emoji", "Segoe UI Emoji";
        color: var(--text);
        background: radial-gradient(1200px 1200px at 80% -10%, #1f2937 5%, var(--bg) 60%);
        display: grid;
        place-items: center;
      }
      .card {
        width: min(92vw, 560px);
        background: linear-gradient(180deg, rgba(255,255,255,0.025), rgba(255,255,255,0.01));
        border: 1px solid rgba(255,255,255,0.08);
        border-radius: 16px;
        padding: 28px;
        box-shadow: 0 20px 60px rgba(0,0,0,0.35), inset 0 1px 0 rgba(255,255,255,0.05);
        backdrop-filter: blur(12px);
      }
      .brand {
        display: flex; align-items: center; gap: 12px; margin-bottom: 4px;
      }
      .logo {
        width: 36px; height: 36px; border-radius: 8px; display: grid; place-items: center;
        background: linear-gradient(135deg, #60a5fa 0%, #22d3ee 50%, #10b981 100%);
        color: #0a0a0a; font-weight: 800;
      }
      h1 { margin: 0; font-size: 1.4rem; letter-spacing: 0.2px; }
      p.muted { margin: 6px 0 18px; color: var(--muted); }

      .row { display: flex; align-items: center; gap: 12px; flex-wrap: wrap; }
      button {
        appearance: none; cursor: pointer; border: none; border-radius: 10px; padding: 10px 14px;
        background: var(--brand); color: white; font-weight: 600; display: inline-flex; align-items: center; gap: 10px;
        box-shadow: 0 8px 24px rgba(37,99,235,0.35);
        transition: transform 120ms ease, background 120ms ease, box-shadow 120ms ease, opacity 120ms ease;
      }
      button:hover { background: var(--brand-hover); transform: translateY(-1px); }
      button.secondary {
        background: transparent; color: var(--text); border: 1px solid rgba(255,255,255,0.12);
        box-shadow: none;
      }
      button:disabled { opacity: 0.6; cursor: not-allowed; }

      .user {
        margin-top: 20px; display: none; gap: 14px; align-items: center; padding: 12px; border: 1px dashed rgba(255,255,255,0.15);
        border-radius: 12px; background: rgba(255,255,255,0.02);
      }
      .user img { width: 48px; height: 48px; border-radius: 999px; border: 1px solid rgba(255,255,255,0.18); }
      .user .meta { display: grid; }
      .status { margin-top: 14px; font-size: 0.95rem; color: var(--muted); min-height: 1.2em; }
      .warn { margin-top: 14px; color: #fde68a; background: rgba(253, 224, 71, 0.12); border: 1px solid rgba(253, 224, 71, 0.25); padding: 10px 12px; border-radius: 10px; display: none; }
      footer { margin-top: 22px; font-size: 12px; color: var(--muted); text-align: center; }
      a { color: #93c5fd; text-decoration: none; }
      a:hover { text-decoration: underline; }
    </style>
  </head>
  <body>
    <main class="card" aria-labelledby="title">
      <div class="brand">
        <div class="logo" aria-hidden="true">SF</div>
        <h1 id="title">School Flow</h1>
      </div>
      <p class="muted">Sign in with your Google account to continue.</p>

      <div class="row">
        <button id="signInBtn" aria-label="Sign in with Google">
          <svg width="18" height="18" viewBox="0 0 24 24" fill="none" aria-hidden="true">
            <path d="M23.5 12.3c0-.84-.07-1.45-.22-2.08H12.24v3.77h6.46c-.13.94-.84 2.35-2.42 3.3l-.02.12 3.51 2.72.24.02c2.26-2.08 3.49-5.16 3.49-8.17Z" fill="#4285F4"/>
            <path d="M12.24 23.5c3.17 0 5.83-1.03 7.77-2.8l-3.71-2.87c-.99.65-2.3 1.1-4.06 1.1a7.05 7.05 0 0 1-6.67-4.9l-.12.01-3.63 2.81-.05.11A11.98 11.98 0 0 0 12.24 23.5Z" fill="#34A853"/>
            <path d="M5.57 13.93A7.24 7.24 0 0 1 5.16 12c0-.68.12-1.33.33-1.93l-.01-.13-3.67-2.85-.12.05A11.98 11.98 0 0 0 .25 12c0 1.92.46 3.74 1.28 5.33l4.04-3.4Z" fill="#FBBC05"/>
            <path d="M12.24 4.92c2.2 0 3.69.94 4.54 1.72l3.32-3.24C18.05 1.47 15.42.5 12.24.5 7.54.5 3.49 3.24 1.53 6.67l4.02 3.4c.99-2.89 3.69-5.15 6.69-5.15Z" fill="#EA4335"/>
          </svg>
          Continue with Google
        </button>
        <button id="signOutBtn" class="secondary" style="display:none" aria-label="Sign out">Sign out</button>
      </div>

      <div id="setupWarning" class="warn">Firebase config is not set. Open <strong>scripts/firebaseConfig.js</strong> and paste your project config from the Firebase Console. Also enable the Google provider and add <code>localhost</code> to Authorized domains.</div>
      <div id="status" class="status" role="status"></div>

      <section id="user" class="user" aria-live="polite">
        <img id="userPhoto" alt="User photo" src="" />
        <div class="meta">
          <strong id="userName">—</strong>
          <span id="userEmail" class="muted">—</span>
        </div>
      </section>

      <footer>
        By continuing you agree to the School Flow terms. Need help? See the README for setup steps.
      </footer>
    </main>

    <noscript>This page requires JavaScript to use Google Sign-In.</noscript>
    <script type="module" src="./scripts/app.js"></script>
  </body>
</html>

